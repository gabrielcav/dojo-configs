eureka:
  instance:
    hostname: consultaserver
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
    server:
      wait-time-in-ms-when-sync-empty: 3000
      enableSelfPreservation: false